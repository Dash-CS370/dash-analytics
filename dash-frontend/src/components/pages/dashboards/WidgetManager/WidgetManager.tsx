import React from 'react';

// TODO: Add widget manager functionality
// --> takes in a list of configs and renders the widgets in grid, manages widget states
export const WidgetManager: React.FC = () => {
    return (
        <div>
            <h1>Widget Manager</h1>
        </div>
    );
};
